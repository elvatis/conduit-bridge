{
  "name": "@elvatis/conduit-bridge",
  "version": "0.2.0",
  "description": "Standalone OpenAI-compatible AI proxy — Grok, Claude, Gemini, ChatGPT via headless browser",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "conduit-bridge": "./dist/cli.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc --noEmit && esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm --platform=node --packages=external --sourcemap && esbuild src/cli.ts --bundle --outfile=dist/cli.js --format=esm --platform=node --packages=external --sourcemap && node -e \"try{require('fs').chmodSync('dist/cli.js',0o755)}catch{}\"",
    "dev": "node --watch dist/cli.js",
    "test": "node --experimental-vm-modules node_modules/.bin/vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.79.0",
    "@google/generative-ai": "^0.24.1",
    "openai": "^6.32.0",
    "playwright": "^1.44.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.4.0",
    "vitest": "^4.1.2"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "ai",
    "grok",
    "claude",
    "gemini",
    "chatgpt",
    "openai",
    "proxy",
    "bridge"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/elvatis/conduit-bridge.git"
  }
}
